// @generated by OpenAPI Generator 7.22.0; DO NOT EDIT.
// Canonical source contract SHA-256: 9e308ca862f77b4670529465fe813351420062524b13b7bdf13bb50442e0b3d9
// CLI policy overlay: problem actions remain opaque; RFC 3339 date-times remain strings.

/*
 * Scherzo Cloud API
 *
 * The public programmatic API for Scherzo Cloud.  This contract exposes authenticated principal lookup, explicit human signup, self-service human profile and identity management, and the initial organization tenant boundary. A caller authenticates with an OAuth 2.0 access token issued by an authorization server configured for the deployment and intended for this API.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use super::{ContentType, Error, configuration};
use crate::api::generated::{apis::ResponseContent, models};
use reqwest;
use serde::{Deserialize, Serialize, de::Error as _};

/// struct for typed errors of method [`create_service_principal`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateServicePrincipalError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status409(models::Problem),
    Status429(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_current_principal`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetCurrentPrincipalError {
    Status401(models::Problem),
    Status403(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`issue_current_service_credential`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum IssueCurrentServiceCredentialError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status409(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`link_current_principal_identity`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum LinkCurrentPrincipalIdentityError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status409(models::Problem),
    Status415(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`list_current_principal_identities`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ListCurrentPrincipalIdentitiesError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`list_current_service_credentials`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ListCurrentServiceCredentialsError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`remove_current_principal_identity`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum RemoveCurrentPrincipalIdentityError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status404(models::Problem),
    Status409(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`revoke_current_service_credential`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum RevokeCurrentServiceCredentialError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status404(models::Problem),
    Status409(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`signup_human`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum SignupHumanError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status409(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`update_current_principal`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum UpdateCurrentPrincipalError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status409(models::Problem),
    Status415(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// An active human principal creates a named service principal. The initial API key is returned only on the first completed response; an exact replay returns the same principal and credential metadata without apiKey.
pub fn create_service_principal(
    configuration: &configuration::Configuration,
    idempotency_key: &str,
    create_service_principal_request: models::CreateServicePrincipalRequest,
) -> Result<models::CreateServicePrincipalResponse, Error<CreateServicePrincipalError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_header_idempotency_key = idempotency_key;
    let p_body_create_service_principal_request = create_service_principal_request;

    let uri_str = format!("{}/v1/service-principals", configuration.base_path);
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.header("Idempotency-Key", p_header_idempotency_key.to_string());
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_create_service_principal_request);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text()?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::CreateServicePrincipalResponse`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::CreateServicePrincipalResponse`"
                ))));
            }
        }
    } else {
        let content = resp.text()?;
        let entity: Option<CreateServicePrincipalError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Returns the active human or service principal associated with the bearer token. This operation is read-only and never provisions a principal. An authenticated human identity that has not completed explicit signup receives a principal-not-provisioned response.
pub fn get_current_principal(
    configuration: &configuration::Configuration,
) -> Result<models::CurrentPrincipal, Error<GetCurrentPrincipalError>> {
    let uri_str = format!("{}/v1/me", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text()?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::CurrentPrincipal`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::CurrentPrincipal`"
                ))));
            }
        }
    } else {
        let content = resp.text()?;
        let entity: Option<GetCurrentPrincipalError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Issues a non-expiring Scherzo platform credential for the authenticated service. The request must have no body and must itself use a platform credential. The API key is returned only on the first completed request; replay returns the same metadata with apiKey omitted.
pub fn issue_current_service_credential(
    configuration: &configuration::Configuration,
    idempotency_key: &str,
) -> Result<models::ServiceCredentialSecret, Error<IssueCurrentServiceCredentialError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_header_idempotency_key = idempotency_key;

    let uri_str = format!("{}/v1/me/credentials", configuration.base_path);
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.header("Idempotency-Key", p_header_idempotency_key.to_string());
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text()?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::ServiceCredentialSecret`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::ServiceCredentialSecret`"
                ))));
            }
        }
    } else {
        let content = resp.text()?;
        let entity: Option<IssueCurrentServiceCredentialError> =
            serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Humans link a proposed human OIDC identity using a fresh separate token. Services must authenticate with a platform credential and prove a fresh token from a configured workload issuer. Workload tokens cannot link.  Every identity already linked to any principal produces the same generic identity-unavailable conflict. The operation never merges principals or identifies an existing holder.  Idempotency-Key is scoped to the exact acting OIDC identity or platform credential and a semantic fingerprint containing the proposed token's SHA-256 digest. Exact completed replay returns the original created response before proposed-token expiry or freshness is revalidated. Reusing the key with another token conflicts. Access tokens are never persisted in identity, audit, log, problem, or idempotency result data.
pub fn link_current_principal_identity(
    configuration: &configuration::Configuration,
    idempotency_key: &str,
    link_oidc_identity_request: models::LinkOidcIdentityRequest,
) -> Result<models::OidcIdentityLink, Error<LinkCurrentPrincipalIdentityError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_header_idempotency_key = idempotency_key;
    let p_body_link_oidc_identity_request = link_oidc_identity_request;

    let uri_str = format!("{}/v1/me/identities", configuration.base_path);
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.header("Idempotency-Key", p_header_idempotency_key.to_string());
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_link_oidc_identity_request);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text()?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::OidcIdentityLink`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::OidcIdentityLink`"
                ))));
            }
        }
    } else {
        let content = resp.text()?;
        let entity: Option<LinkCurrentPrincipalIdentityError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Returns only OIDC links belonging to the active principal. Human items include retained email provenance; service workload items omit it. Workload authentication marks its exact issuer-subject current, while a platform credential marks no workload link current. Listing is read-only.  Results are ordered from oldest to newest by creation time and opaque identity-link ID. The cursor is opaque and may be used only with this operation. If the exact authenticating identity is no longer linked when the listing query evaluates authorization, the request is forbidden and no identity metadata is returned.
pub fn list_current_principal_identities(
    configuration: &configuration::Configuration,
    limit: Option<i32>,
    cursor: Option<&str>,
) -> Result<models::OidcIdentityList, Error<ListCurrentPrincipalIdentitiesError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_query_limit = limit;
    let p_query_cursor = cursor;

    let uri_str = format!("{}/v1/me/identities", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_query_limit {
        req_builder = req_builder.query(&[("limit", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_cursor {
        req_builder = req_builder.query(&[("cursor", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text()?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::OidcIdentityList`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::OidcIdentityList`"
                ))));
            }
        }
    } else {
        let content = resp.text()?;
        let entity: Option<ListCurrentPrincipalIdentitiesError> =
            serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Returns active non-secret credential metadata owned by the authenticated service, oldest first. The credential used for this request is marked current. Revoked credentials are retained but omitted from this list.
pub fn list_current_service_credentials(
    configuration: &configuration::Configuration,
    limit: Option<i32>,
    cursor: Option<&str>,
) -> Result<models::ServiceCredentialList, Error<ListCurrentServiceCredentialsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_query_limit = limit;
    let p_query_cursor = cursor;

    let uri_str = format!("{}/v1/me/credentials", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_query_limit {
        req_builder = req_builder.query(&[("limit", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_cursor {
        req_builder = req_builder.query(&[("cursor", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text()?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::ServiceCredentialList`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::ServiceCredentialList`"
                ))));
            }
        }
    } else {
        let content = resp.text()?;
        let entity: Option<ListCurrentServiceCredentialsError> =
            serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Humans remove a non-current human identity using another fresh identity. Services remove an owned workload identity using a platform credential. The request accepts no body.  The current or last usable identity cannot be removed. A nonexistent identity and an identity belonging to another principal produce the same not-found response. Removing a link releases its issuer-subject for later explicit signup or proof-based linking without restoring its former principal, while the removed identity-link ID remains permanently reserved.  Idempotency-Key is scoped to the exact acting OIDC identity and a semantic fingerprint containing identityId. Exact completed replay returns the original 204 response before bearer freshness is reevaluated. Reusing the key for another target conflicts. Rejected attempts are reevaluated.
pub fn remove_current_principal_identity(
    configuration: &configuration::Configuration,
    identity_id: &str,
    idempotency_key: &str,
) -> Result<(), Error<RemoveCurrentPrincipalIdentityError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_identity_id = identity_id;
    let p_header_idempotency_key = idempotency_key;

    let uri_str = format!(
        "{}/v1/me/identities/{identityId}",
        configuration.base_path,
        identityId = crate::api::generated::apis::urlencode(p_path_identity_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.header("Idempotency-Key", p_header_idempotency_key.to_string());
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text()?;
        let entity: Option<RemoveCurrentPrincipalIdentityError> =
            serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Revokes an owned active credential immediately while retaining its ID and digest. The request must have no body and must use a different active platform credential; a credential cannot revoke itself.
pub fn revoke_current_service_credential(
    configuration: &configuration::Configuration,
    credential_id: &str,
    idempotency_key: &str,
) -> Result<(), Error<RevokeCurrentServiceCredentialError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_credential_id = credential_id;
    let p_header_idempotency_key = idempotency_key;

    let uri_str = format!(
        "{}/v1/me/credentials/{credentialId}",
        configuration.base_path,
        credentialId = crate::api::generated::apis::urlencode(p_path_credential_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.header("Idempotency-Key", p_header_idempotency_key.to_string());
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text()?;
        let entity: Option<RevokeCurrentServiceCredentialError> =
            serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Explicitly provisions a human principal for the authenticated identity when the deployment's platform signup policy permits open signup. The request has no body; identity and optional profile attributes come only from verified access-token claims.  Idempotency-Key is required. Keys are scoped to the authenticated identity and this operation and are retained for seven days. Retrying with the same key returns the original 201 response without repeating the principal or audit-record mutation. After signup, a new key for the same identity receives a conflict response.
pub fn signup_human(
    configuration: &configuration::Configuration,
    idempotency_key: &str,
) -> Result<models::CurrentPrincipal, Error<SignupHumanError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_header_idempotency_key = idempotency_key;

    let uri_str = format!("{}/v1/signup", configuration.base_path);
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.header("Idempotency-Key", p_header_idempotency_key.to_string());
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text()?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::CurrentPrincipal`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::CurrentPrincipal`"
                ))));
            }
        }
    } else {
        let content = resp.text()?;
        let entity: Option<SignupHumanError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Updates or clears the active human or service principal's optional display name. A string value is normalized by removing surrounding Unicode whitespace before comparison and storage. The normalized value must contain 1 to 200 Unicode scalar values and no Unicode control characters. A null value clears the display name. The patch must contain displayName and no unknown properties.  A normalized value equal to the stored value succeeds without changing the principal update timestamp or appending an audit record. Actual changes serialize on the principal and use last-committed-write-wins semantics.  Idempotency-Key is required and scoped to the exact authenticated OIDC identity or platform credential and this operation. The semantic fingerprint contains the normalized nullable display name. Retrying the same request and key within seven days returns the original status, headers, and body without repeating the mutation or audit event.
pub fn update_current_principal(
    configuration: &configuration::Configuration,
    idempotency_key: &str,
    update_current_principal_patch: models::UpdateCurrentPrincipalPatch,
) -> Result<models::CurrentPrincipal, Error<UpdateCurrentPrincipalError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_header_idempotency_key = idempotency_key;
    let p_body_update_current_principal_patch = update_current_principal_patch;

    let uri_str = format!("{}/v1/me", configuration.base_path);
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::PATCH, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.header("Idempotency-Key", p_header_idempotency_key.to_string());
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_update_current_principal_patch);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req)?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text()?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::CurrentPrincipal`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::CurrentPrincipal`"
                ))));
            }
        }
    } else {
        let content = resp.text()?;
        let entity: Option<UpdateCurrentPrincipalError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}
