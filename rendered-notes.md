## Added

- Add command and agent handlers for Workflow V1 step recovery.
- Add local workflow step recovery with durable status, results, and explicit retry isolation.

## Fixed

- Make Runner Serve lease authority suspend-aware and independent of host wall-clock time.
- Fix CLI compilation on macOS.
- Fix Codex structured workflow results failing before model execution.
