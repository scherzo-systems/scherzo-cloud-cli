```console
$ scherzo-cloud runner credential --help
Manage runner credential lifecycle

Usage: scherzo-cloud runner credential [COMMAND]

Commands:
  list    List runner credential metadata
  retire  Schedule runner credential retirement
  revoke  Revoke a runner credential
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help

```
