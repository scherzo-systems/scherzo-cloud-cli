```console
$ scherzo-cloud project create --help
Create a Scherzo Cloud project

Usage: scherzo-cloud project create [OPTIONS] --name <NAME> --installation-id <INSTALLATION> --repository-id <REPOSITORY> <ORGANIZATION>

Arguments:
  <ORGANIZATION>  Organization ID or exact slug

Options:
      --name <NAME>
          Set the canonical project name
      --installation-id <INSTALLATION>
          Exact GitHub installation binding ID
      --repository-id <REPOSITORY>
          Exact provider repository ID
      --default-branch <DEFAULT_BRANCH>
          Set the configured default branch (the provider default when omitted)
      --runner-pool-id <POOL>
          Assign an exact runner pool ID
      --json
          Print the project result as JSON
      --allow-insecure-http
          Allow this command's Scherzo Cloud requests over insecure HTTP connections
  -h, --help
          Print help

```
