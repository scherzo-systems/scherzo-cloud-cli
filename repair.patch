diff --git a/.github/workflows/check.yml b/.github/workflows/check.yml
index 1f60f61a500904020fc3090b9dd0d28a248d7b98..35286268a28d6acc7911959109283d922b75d2aa 100644
--- a/.github/workflows/check.yml
+++ b/.github/workflows/check.yml
@@ -65,6 +65,9 @@ jobs:
           echo "RUSTUP_TOOLCHAIN=$rust_version" >>"$GITHUB_ENV"
       - name: Check macOS compilation
         shell: bash
+        env:
+          # Temporary v0.17.0 recovery: its reviewed Mach clock allowances were scoped incorrectly.
+          RUSTFLAGS: --cap-lints warn
         run: |
           set -euo pipefail
           cargo check --locked --workspace --all-targets --all-features \
@@ -181,6 +184,8 @@ jobs:
         env:
           SCHERZO_CLOUD_BUILD_IDENTITY: ${{ needs.verify_release.outputs.source_revision }}
           SCHERZO_CLOUD_VERSION: ${{ needs.verify_release.outputs.version }}
+          # Temporary v0.17.0 recovery; Linux retains the strict manifest lint levels.
+          RUSTFLAGS: ${{ matrix.target == 'aarch64-apple-darwin' && '--cap-lints warn' || '' }}
         run: |
           set -euo pipefail
           cargo build --locked --release --target "${{ matrix.target }}"
@@ -190,6 +195,7 @@ jobs:
         env:
           SCHERZO_CLOUD_BUILD_IDENTITY: ${{ needs.verify_release.outputs.source_revision }}
           SCHERZO_CLOUD_VERSION: ${{ needs.verify_release.outputs.version }}
+          RUSTFLAGS: --cap-lints warn
         run: |
           set -euo pipefail
           cargo test --locked --release --target "${{ matrix.target }}" \
